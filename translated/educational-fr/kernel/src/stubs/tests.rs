//! Hypervisor tests stub
use alloc::string::String;
use alloc::vec::Vec;

// Fonction publique — appelable depuis d'autres modules.
pub fn run_all_tests() -> (usize, usize, Vec<String>) {
    (0, 0, Vec::new())
}
